import styled from "styled-components";

const StyledArchiveCard = styled.div`
  display: grid;
  grid-template-columns: 70px 100px 80px 100px 70px 120px 1fr;
  color: white;
  overflow-x: scroll;
  white-space: normal;
  word-break: normal;
  justify-content: start;
  border: 1px solid rgba(165, 202, 210, 0.4);
`;

export default StyledArchiveCard;
